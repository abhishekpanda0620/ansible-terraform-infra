- name: Configure web server
  hosts: web
  become: true

  tasks:
    - name: Install NGINX
      package:
        name: nginx
        state: present

    - name: Ensure NGINX is running
      service:
        name: nginx
        state: started
        enabled: yes
